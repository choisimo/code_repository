//
// Created by csimo on 10/14/2024.
//
#include <stdio.h>

int main() {
    int x,y, max;
    printf("두 수 입력 :");
    scanf_s("%d %d", &x, &y);

    if ((max = x > y ? x : y)) {

    }
}